id: redirect-308-permanent
category: redirects
description: >
  A 308, the newest redirect status and the one oldest clients are most likely to mishandle by
  treating it as an error rather than a redirect.
references: ["RFC 9110 §15.4.9", "INVARIANTS.md#i-8"]

server:
  content: { size: 64KiB, seed: 43 }
  redirect_chain: [308]

expect:
  final_state: completed
  file_renamed: true
  redirect_chain_len: 2
